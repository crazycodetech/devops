git --- init --to set up local repository
        add----put data into local
        commit----save changes(still in local repo)(staging repo)
        push-----local repo to remote repo
        pull---remote to local repo
        checkout-----local to working dir

tools to access git 

1 cloud interface  - giyhub
2. cli-gitcli
